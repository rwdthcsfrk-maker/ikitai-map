# 店舗検索機能 設計ドキュメント

## 1. MVPフィルタセット

MVPでは、ユーザーが最も頻繁に使用する検索条件に絞り込み、シンプルで直感的な検索体験を提供する。

### MVP必須フィルタ（8項目）

| カテゴリ | フィルタ名 | 選択肢 | 備考 |
|---------|-----------|--------|------|
| エリア | 距離（現在地から） | 300m / 1km / 3km / 5km / 指定なし | GPS連携 |
| エリア | 都道府県 | 47都道府県 | マスタ管理 |
| ジャンル | 大ジャンル | カフェ / 和食 / 洋食 / 中華 / アジア・エスニック / 焼肉・肉 / 居酒屋・バー / ラーメン・麺 / スイーツ | 固定マスタ |
| ジャンル | 小ジャンル | 大ジャンル選択で動的表示 | 親子関係 |
| 営業 | 営業中のみ | ON / OFF | Google Places API連携 |
| 予算 | 予算区分 | ランチ / ディナー | 切り替え式 |
| 予算 | 予算帯 | ランチ: 〜¥1,000 / ¥1,000–2,000 / ¥2,000–3,000 / ¥3,000+ | 区分連動 |
| 並び替え | ソート順 | おすすめ / 距離が近い / 評価が高い / 口コミが多い / 新着 | 単一選択 |

### MVP推奨フィルタ（4項目）

| カテゴリ | フィルタ名 | 選択肢 | 備考 |
|---------|-----------|--------|------|
| こだわり | 禁煙/喫煙 | 完全禁煙 / 分煙 / 喫煙可 / 指定なし | features配列 |
| こだわり | 個室 | あり / なし / 指定なし | features配列 |
| こだわり | テイクアウト | あり / なし / 指定なし | features配列 |
| 設備 | Wi-Fi/電源 | Wi-Fiあり / 電源あり / 両方あり / 指定なし | features配列 |

---

## 2. 拡張フェーズの追加フィルタ案

### Phase 2（優先度: 高）

| フィルタ名 | 選択肢 | 理由 |
|-----------|--------|------|
| 市区町村 | 都道府県選択で動的表示 | エリア絞り込み精度向上 |
| 駅・沿線 | 沿線→駅の2段階選択 | 駅近検索ニーズ |
| 徒歩分数 | 〜5分 / 〜10分 / 〜15分 | 駅選択時に連動 |
| 時間帯 | 朝 / 昼 / 夜 / 深夜 | 営業時間フィルタ |
| 予約可否 | 可 / 不可 / 当日OK | 予約ニーズ対応 |

### Phase 3（優先度: 中）

| フィルタ名 | 選択肢 | 理由 |
|-----------|--------|------|
| 曜日 | 平日 / 土日 / 祝日 | 営業日フィルタ |
| 24時間営業 | ON / OFF | 深夜利用ニーズ |
| デリバリー | あり / なし | Uber Eats等連携 |
| 席タイプ | カウンター / テーブル / テラス | 席指定ニーズ |
| サービス料 | なし / あり | 予算計算用 |

### Phase 4（優先度: 低）

| フィルタ名 | 選択肢 | 理由 |
|-----------|--------|------|
| 駐車場 | あり / なし | 車利用者向け |
| バリアフリー | 対応 / 非対応 | アクセシビリティ |
| 作業向き | はい / いいえ | ノマドワーカー向け |

---

## 3. マスタ設計

### 3.1 ジャンルマスタ（genres）

```typescript
// 大ジャンル（固定）
const PARENT_GENRES = [
  { id: 'cafe', name: 'カフェ', icon: '☕' },
  { id: 'japanese', name: '和食', icon: '🍱' },
  { id: 'western', name: '洋食', icon: '🍝' },
  { id: 'chinese', name: '中華', icon: '🥟' },
  { id: 'asian', name: 'アジア・エスニック', icon: '🍜' },
  { id: 'meat', name: '焼肉・肉', icon: '🥩' },
  { id: 'izakaya', name: '居酒屋・バー', icon: '🍺' },
  { id: 'ramen', name: 'ラーメン・麺', icon: '🍜' },
  { id: 'sweets', name: 'スイーツ', icon: '🍰' },
] as const;

// 小ジャンル（親に紐づく）
const CHILD_GENRES: Record<string, Array<{id: string, name: string}>> = {
  cafe: [
    { id: 'cafe_general', name: 'カフェ' },
    { id: 'cafe_specialty', name: 'スペシャルティコーヒー' },
    { id: 'cafe_chain', name: 'チェーン系カフェ' },
    { id: 'cafe_kissaten', name: '喫茶店' },
  ],
  japanese: [
    { id: 'sushi', name: '寿司' },
    { id: 'tempura', name: '天ぷら' },
    { id: 'kaiseki', name: '懐石・会席' },
    { id: 'udon', name: 'うどん' },
    { id: 'soba', name: 'そば' },
    { id: 'tonkatsu', name: 'とんかつ' },
    { id: 'yakitori', name: '焼き鳥' },
  ],
  western: [
    { id: 'italian', name: 'イタリアン' },
    { id: 'french', name: 'フレンチ' },
    { id: 'spanish', name: 'スペイン料理' },
    { id: 'steak', name: 'ステーキ' },
    { id: 'hamburger', name: 'ハンバーガー' },
    { id: 'pizza', name: 'ピザ' },
  ],
  chinese: [
    { id: 'chinese_general', name: '中華料理' },
    { id: 'chinese_sichuan', name: '四川料理' },
    { id: 'chinese_cantonese', name: '広東料理' },
    { id: 'chinese_dimsum', name: '飲茶・点心' },
  ],
  asian: [
    { id: 'thai', name: 'タイ料理' },
    { id: 'vietnamese', name: 'ベトナム料理' },
    { id: 'korean', name: '韓国料理' },
    { id: 'indian', name: 'インド料理' },
    { id: 'mexican', name: 'メキシカン' },
  ],
  meat: [
    { id: 'yakiniku', name: '焼肉' },
    { id: 'horumon', name: 'ホルモン' },
    { id: 'shabushabu', name: 'しゃぶしゃぶ' },
    { id: 'sukiyaki', name: 'すき焼き' },
  ],
  izakaya: [
    { id: 'izakaya_general', name: '居酒屋' },
    { id: 'tachinomi', name: '立ち飲み' },
    { id: 'beer_bar', name: 'ビアバー' },
    { id: 'wine_bar', name: 'ワインバー' },
    { id: 'cocktail_bar', name: 'カクテルバー' },
    { id: 'whisky_bar', name: 'ウイスキーバー' },
  ],
  ramen: [
    { id: 'ramen_general', name: 'ラーメン' },
    { id: 'tsukemen', name: 'つけ麺' },
    { id: 'tantanmen', name: '担々麺' },
  ],
  sweets: [
    { id: 'cake', name: 'ケーキ' },
    { id: 'parfait', name: 'パフェ' },
    { id: 'wagashi', name: '和菓子' },
    { id: 'crepe', name: 'クレープ' },
    { id: 'ice_cream', name: 'アイスクリーム' },
  ],
};
```

### 3.2 予算帯マスタ（budgetBands）

```typescript
const BUDGET_BANDS = {
  lunch: [
    { id: 'lunch_1', label: '〜¥1,000', min: 0, max: 1000 },
    { id: 'lunch_2', label: '¥1,000–2,000', min: 1000, max: 2000 },
    { id: 'lunch_3', label: '¥2,000–3,000', min: 2000, max: 3000 },
    { id: 'lunch_4', label: '¥3,000+', min: 3000, max: null },
  ],
  dinner: [
    { id: 'dinner_1', label: '〜¥3,000', min: 0, max: 3000 },
    { id: 'dinner_2', label: '¥3,000–5,000', min: 3000, max: 5000 },
    { id: 'dinner_3', label: '¥5,000–8,000', min: 5000, max: 8000 },
    { id: 'dinner_4', label: '¥8,000+', min: 8000, max: null },
  ],
} as const;
```

### 3.3 距離マスタ（distanceOptions）

```typescript
const DISTANCE_OPTIONS = [
  { id: '300m', label: '300m以内', meters: 300 },
  { id: '1km', label: '1km以内', meters: 1000 },
  { id: '3km', label: '3km以内', meters: 3000 },
  { id: '5km', label: '5km以内', meters: 5000 },
  { id: 'any', label: '指定なし', meters: null },
] as const;
```

### 3.4 特徴フラグマスタ（features）

```typescript
const FEATURE_OPTIONS = {
  smoking: [
    { id: 'non_smoking', label: '完全禁煙' },
    { id: 'separated', label: '分煙' },
    { id: 'smoking_ok', label: '喫煙可' },
  ],
  privateRoom: [
    { id: 'private_room_yes', label: 'あり' },
    { id: 'private_room_no', label: 'なし' },
  ],
  takeout: [
    { id: 'takeout_yes', label: 'あり' },
    { id: 'takeout_no', label: 'なし' },
  ],
  wifi: [
    { id: 'wifi_yes', label: 'Wi-Fiあり' },
  ],
  power: [
    { id: 'power_yes', label: '電源あり' },
  ],
} as const;
```

### 3.5 並び替えオプション（sortOptions）

```typescript
const SORT_OPTIONS = [
  { id: 'recommended', label: 'おすすめ' },
  { id: 'distance', label: '距離が近い' },
  { id: 'rating', label: '評価が高い' },
  { id: 'reviews', label: '口コミが多い' },
  { id: 'new', label: '新着' },
] as const;
```

---

## 4. データモデル案

### 4.1 型定義（TypeScript）

```typescript
// 検索フィルタの型定義
interface SearchFilters {
  // エリア・距離
  location?: {
    lat: number;
    lng: number;
  };
  distanceRadius?: number | null; // メートル単位、nullは指定なし
  prefecture?: string;
  city?: string;
  stationId?: string;
  walkMinutes?: number;

  // ジャンル
  genreParent?: string;
  genreChild?: string;

  // 予算
  budgetType?: 'lunch' | 'dinner';
  budgetMin?: number;
  budgetMax?: number | null;

  // 営業
  openNow?: boolean;
  timeBand?: 'morning' | 'lunch' | 'dinner' | 'late_night';
  dayType?: 'weekday' | 'weekend' | 'holiday';

  // 特徴（複数選択可）
  features?: string[];

  // 並び替え
  sort?: 'recommended' | 'distance' | 'rating' | 'reviews' | 'new';

  // ページネーション
  page?: number;
  limit?: number;
}

// 検索結果の型定義
interface SearchResult {
  places: PlaceWithDistance[];
  total: number;
  page: number;
  limit: number;
  hasMore: boolean;
}

interface PlaceWithDistance {
  id: number;
  name: string;
  address: string | null;
  lat: number | null;
  lng: number | null;
  genre: string | null;
  genreParent: string | null;
  genreChild: string | null;
  features: string[];
  summary: string | null;
  rating: number | null;
  reviewCount: number | null;
  budgetLunch: string | null;
  budgetDinner: string | null;
  googleMapsUrl: string | null;
  distance?: number; // メートル単位（現在地指定時のみ）
  status: 'none' | 'want_to_go' | 'visited';
  userRating: number | null;
}
```

### 4.2 DBテーブル拡張案

```sql
-- placesテーブルの拡張カラム
ALTER TABLE places ADD COLUMN genre_parent VARCHAR(50);
ALTER TABLE places ADD COLUMN genre_child VARCHAR(50);
ALTER TABLE places ADD COLUMN budget_lunch VARCHAR(20);
ALTER TABLE places ADD COLUMN budget_dinner VARCHAR(20);
ALTER TABLE places ADD COLUMN rating DECIMAL(2,1);
ALTER TABLE places ADD COLUMN review_count INT DEFAULT 0;

-- インデックス追加
CREATE INDEX idx_places_genre ON places(genre_parent, genre_child);
CREATE INDEX idx_places_location ON places(lat, lng);
CREATE INDEX idx_places_rating ON places(rating DESC);
```

---

## 5. API設計案

### 5.1 検索エンドポイント

```
POST /api/trpc/search.filter
```

### 5.2 リクエスト例

```json
{
  "location": {
    "lat": 35.6812,
    "lng": 139.7671
  },
  "distanceRadius": 1000,
  "genreParent": "izakaya",
  "genreChild": "wine_bar",
  "budgetType": "dinner",
  "budgetMin": 3000,
  "budgetMax": 5000,
  "openNow": true,
  "features": ["non_smoking", "private_room_yes"],
  "sort": "rating",
  "page": 1,
  "limit": 20
}
```

### 5.3 レスポンス例

```json
{
  "places": [
    {
      "id": 123,
      "name": "ワインバー VINO",
      "address": "東京都渋谷区道玄坂1-2-3",
      "lat": 35.6580,
      "lng": 139.7016,
      "genre": "ワインバー",
      "genreParent": "izakaya",
      "genreChild": "wine_bar",
      "features": ["non_smoking", "private_room_yes", "wifi_yes"],
      "summary": "落ち着いた雰囲気のワインバー。個室完備でデートにも最適。",
      "rating": 4.2,
      "reviewCount": 128,
      "budgetLunch": null,
      "budgetDinner": "dinner_2",
      "googleMapsUrl": "https://maps.google.com/?cid=xxx",
      "distance": 850,
      "status": "want_to_go",
      "userRating": null
    }
  ],
  "total": 45,
  "page": 1,
  "limit": 20,
  "hasMore": true
}
```

### 5.4 マスタ取得エンドポイント

```
GET /api/trpc/master.genres      // ジャンルマスタ
GET /api/trpc/master.budgets     // 予算帯マスタ
GET /api/trpc/master.prefectures // 都道府県マスタ
```

---

## 6. UIワイヤー（文章版）

### 6.1 画面構成

```
┌─────────────────────────────────────────────────────────┐
│ ヘッダー                                                │
│ [← 戻る] 店舗を探す                    [フィルタ件数]  │
├─────────────────────────────────────────────────────────┤
│ 検索バー（自然言語検索も可能）                          │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ 🔍 キーワードで検索...                              │ │
│ └─────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────┤
│ フィルタチップ（選択中の条件を表示）                    │
│ [渋谷区 ×] [イタリアン ×] [個室あり ×] [+フィルタ]    │
├─────────────────────────────────────────────────────────┤
│ 並び替え                                                │
│ [おすすめ ▼]                              45件の結果   │
├─────────────────────────────────────────────────────────┤
│ 結果一覧（カード形式）                                  │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ 🍝 トラットリア・ダ・ルイジ                        │ │
│ │ ⭐ 4.2 (128件) | 渋谷区 | 850m                      │ │
│ │ イタリアン | ディナー ¥3,000–5,000                  │ │
│ │ [個室] [禁煙] [Wi-Fi]                               │ │
│ │ 「本格ナポリピザが自慢の隠れ家イタリアン」          │ │
│ └─────────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ ...                                                 │ │
│ └─────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

### 6.2 フィルタ画面（ボトムシート）

```
┌─────────────────────────────────────────────────────────┐
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│                                                         │
│ 絞り込み条件                    [クリア] [適用 (12件)] │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ ▼ エリア・距離                                         │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ 現在地からの距離                                    │ │
│ │ [指定なし ▼]                                        │ │
│ │                                                     │ │
│ │ 都道府県                                            │ │
│ │ [東京都 ▼]                                          │ │
│ └─────────────────────────────────────────────────────┘ │
│                                                         │
│ ▼ ジャンル                                             │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ 大ジャンル                                          │ │
│ │ [洋食 ▼]                                            │ │
│ │                                                     │ │
│ │ 小ジャンル                                          │ │
│ │ [イタリアン ▼]                                      │ │
│ └─────────────────────────────────────────────────────┘ │
│                                                         │
│ ▼ 予算                                                 │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ [ランチ] [ディナー]  ← タブ切り替え                 │ │
│ │                                                     │ │
│ │ 予算帯                                              │ │
│ │ [¥3,000–5,000 ▼]                                    │ │
│ └─────────────────────────────────────────────────────┘ │
│                                                         │
│ ▼ 営業状況                                             │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ 営業中のみ表示                                      │ │
│ │ [━━━━━●] ON                                         │ │
│ └─────────────────────────────────────────────────────┘ │
│                                                         │
│ ▼ こだわり条件                                         │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ 禁煙/喫煙        [完全禁煙 ▼]                       │ │
│ │ 個室            [あり ▼]                            │ │
│ │ テイクアウト    [指定なし ▼]                        │ │
│ │ Wi-Fi/電源      [Wi-Fiあり ▼]                       │ │
│ └─────────────────────────────────────────────────────┘ │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 6.3 操作フロー

1. **検索画面を開く**: ホーム画面の検索バーをタップ、または「探す」タブを選択
2. **フィルタを設定**: 「+フィルタ」ボタンでボトムシートを開き、条件を選択
3. **条件を適用**: 「適用」ボタンで検索実行、結果一覧に遷移
4. **並び替え**: 結果一覧上部のドロップダウンで並び順を変更
5. **条件を変更**: フィルタチップの「×」で個別解除、または「+フィルタ」で再設定
6. **店舗を選択**: カードをタップで詳細表示、Googleマップへ遷移可能
7. **保存**: 詳細画面から「保存」「行きたい」などのアクションを実行

### 6.4 レスポンシブ対応

**モバイル（〜768px）:**
- フィルタはフルスクリーンのボトムシート
- 結果は1カラムのカードリスト
- 並び替えはボトムシートで選択

**タブレット/デスクトップ（768px〜）:**
- フィルタは左サイドバー（折りたたみ可）
- 結果は2〜3カラムのグリッド表示
- 並び替えはドロップダウン

---

## 7. 実装優先順位

### Sprint 1（MVP）
1. マスタデータの定義（ジャンル、予算帯、距離）
2. 検索APIの実装（基本フィルタ対応）
3. フィルタUIの実装（ボトムシート）
4. 結果一覧UIの実装（カード表示）
5. 並び替え機能の実装

### Sprint 2（推奨機能）
1. 都道府県・市区町村フィルタ
2. こだわり条件（禁煙、個室、テイクアウト）
3. Wi-Fi/電源フィルタ
4. フィルタチップUI

### Sprint 3（拡張機能）
1. 駅・沿線検索
2. 時間帯・曜日フィルタ
3. 予約可否フィルタ
4. 検索履歴・お気に入り条件保存
